#!/usr/bin/env bash
FLABULESS_ROOT=$HOME/Documents/Projects/flabuless/

# clone code
git clone git@github.com:rewardz/flabuless $FLABULESS_ROOT

# Now we need to create a virtualenv

# Now we need to create a virtualenv and stuff.
# We assume pyenv and nvm are already installed and just configure it 
# for this particular project
